apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-ventas
  namespace: proyecto-semestral
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-ventas
  template:
    metadata:
      labels:
        app: backend-ventas
    spec:
      containers:
        - name: backend-ventas
          image: proyecto-semestral-back-ventas:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

          env:
            - name: DB_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: mysql-config
                  key: DB_ENDPOINT

            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: mysql-config
                  key: DB_PORT

            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: mysql-config
                  key: DB_NAME

            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: DB_USERNAME

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: DB_PASSWORD

          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "700m"
              memory: "1Gi"

          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 45
            periodSeconds: 10

          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 70
            periodSeconds: 20

---
apiVersion: v1
kind: Service
metadata:
  name: backend-ventas-service
  namespace: proyecto-semestral
spec:
  type: ClusterIP
  selector:
    app: backend-ventas
  ports:
    - name: http
      port: 8080
      targetPort: 8080